<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kemaskini Task</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container py-5">
    <div class="card mx-auto" style="max-width: 500px;">
      <div class="card-body">
        <h2 class="card-title mb-4">Kemaskini Task</h2>
        <form action="/edit/<%= task.id %>" method="POST">
          <div class="mb-3">
            <label for="title" class="form-label">Tajuk Tugasan</label>
            <input type="text" id="title" name="title" class="form-control" value="<%= task.title %>" required>
          </div>
          <div class="mb-3">
            <label for="name" class="form-label">Nama Pekerja</label>
            <input type="text" id="name" name="name" class="form-control" value="<%= task.name %>" required>
          </div>
          <div class="mb-3">
            <label for="role" class="form-label">Peranan Pekerja</label>
            <input type="text" id="role" name="role" class="form-control" value="<%= task.role %>" required>
          </div>
          <button type="submit" class="btn btn-success w-100 mb-2">Simpan</button>
          <a href="/" class="btn btn-secondary w-100">Batal</a>
        </form>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 